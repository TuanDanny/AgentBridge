# AgentBridge Windows MCP Setup

This guide explains how to connect AgentBridge to Codex through a local MCP server on Windows.

## Prerequisites

- Node.js installed
- AgentBridge cloned locally
- Dependencies installed with `npm install`
- Project built with `npm run build`

## Build and test

```powershell
cd D:\AgentBridge
npm install
npm run build
npm test